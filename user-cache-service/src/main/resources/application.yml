# User Cache Service - Main Configuration
# This is the entry point for the application configuration

spring:
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
    allow-circular-references: true

# Import specific configuration files
---
spring:
  config:
    import:
      - optional:classpath:/application-${spring.profiles.active}.yml
